@model IEnumerable<Demo.Models.Book>

@{
    ViewData["Title"] = "Home Page";
}

<br>
<div class="container col-md-10 text-center text-light">
    <div class="container mt-4">
        <div class="row">
            <div class="col">
                <form asp-controller="Search" asp-action="Index">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search for..." name="SearchString">
                    </div>
                </form>
            </div>
        </div>
        <br>
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-3">
                    <div class="card mb-4 bg-dark">
                        <a asp-action="Details" asp-route-id="@item.ProductID">
                            <img src="@Html.DisplayFor(modelItem => item.ImageURL)" class="w-100" />
                        </a>
                        <div class="card-body">
                            <h5 class="card-title">@item.ProductName</h5>
                            <p class="card-text">$@item.Price</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

